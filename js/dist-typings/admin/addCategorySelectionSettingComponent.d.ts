/**
 * Registers the `fof-blog.select-categories` custom setting type used by the
 * declarative `blog_tags` setting registration.
 */
export default function addCategorySelectionSettingComponent(): void;
